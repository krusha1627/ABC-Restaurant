package com.ABC.ABC_Restaurant.repo;

import com.ABC.ABC_Restaurant.entity.RService;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RServiceRepository extends JpaRepository<RService, Long> {
}
